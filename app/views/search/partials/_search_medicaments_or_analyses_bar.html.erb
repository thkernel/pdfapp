<div class="card">
    <div class="card-body text-center">
        <%= form_with(url: search_medicaments_or_analyses_path, method: "get", class: "search-form", local: true) do |f| %>
              
            <div class="row ">               
                <div class="col-md-9">
                    <div class="form-group">
                        <%= f.text_field(:misc,  class: "form-control", placeholder: "Saisir un médicament ou une analyse", value: (@misc if @misc.present?),  required: true) %>
                    </div>
                </div>
              
                                
                <div class="col-md-3">
                    <div class="form-group">
                        <%= f.submit("RECHERCHER", class: "btn btn-primary btn-block") %>
                    </div>
                </div>
                          
            </div>
        <% end %>
    </div>
</div>